import AuthForm from "@/components/authentication/AuthForm";

const Auth = () => {
  return (
    <main className="w-[100vw] authbg h-[100vh] flex flex-col items-center justify-center">
      <AuthForm />
    </main>
  );
};

export default Auth;
